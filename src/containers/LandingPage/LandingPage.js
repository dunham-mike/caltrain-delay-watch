import React, { useContext } from 'react';
import { Link as RouterLink } from 'react-router-dom';

const LandingPage = (props) => {    
    return (
        <div class="has-text-white is-size-5 has-text-centered has-text-weight-bold" style={{ maxWidth: '850px', width: '100%', margin: '0 auto', padding: '0.75rem 1.5rem' }}>
            Caltrain Delay Watch Landing Page
        </div>
    );
}

export default LandingPage;